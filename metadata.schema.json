{
    "type":  "object",
    "properties": {
        "enabled": {
            "description": "Whether to include this package in workflows.",
            "type": "boolean"
        },
        "target": {
            "enum": ["python", "node", "django", "exe"]
        }
    },
    "required": ["enabled"]
}
